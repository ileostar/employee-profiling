export type LoginReg = {
    username: string
    password: string
}

export type fixPassword = {
    id: number
    password: string
}
export type fixUsername = {
    id: number
    username: string
}

export type Tag = {
    id?: number
    features?: string
}